## Ejecución del Código en JavaScript

1. Abre una terminal y navega al directorio donde se encuentra el archivo `matriz-multidimensional.js`.
2. Ejecuta el siguiente comando:

   ```bash
   node matriz-multidimensional.js
   ```
